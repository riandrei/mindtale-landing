{
  "name": "mindtale-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "npm run build && npx serve dist",
    "preview": "vite preview"
  },
  "devDependencies": {},
  "dependencies": {
    "vite": "^6.3.5",
    "serve": "^14.2.4"
  }
}
